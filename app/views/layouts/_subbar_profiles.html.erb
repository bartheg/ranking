<div class="btn-toolbar" role="toolbar" aria-label="...">
  <div class="btn-group" role="group" aria-label="...">
    <%= link_to 'All', profiles_path, class: "btn" %>
    <% if user_signed_in? %>
      <%= link_to 'Participated', '#', class: "btn" %>
      <%= link_to 'My profiles', user_profiles_path(current_user), class: "btn" %>
      <%= link_to 'Create a new profile', new_profile_path, class: "btn btn-warning" %>
    <% end %>
  </div>
</div>
